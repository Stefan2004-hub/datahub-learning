source:
  type: postgres
  config:
    host_port: localhost:5432
    database: inventory
    username: myuser
    password: secret

sink:
  type: datahub-rest
  config:
    server: http://localhost:8080
